# Leave db.driver blank to let the connection pool auto-select the PostgreSQL driver.
db.driver=
db.url=jdbc:postgresql://localhost:5432/retail?sslmode=disable
db.username=postgres
db.password=postgres
db.pool.initial=5
db.pool.max=20
db.connection.timeout=30000
db.validation.timeout=5000
